apiVersion: v1
kind: Service
metadata:
  name: story-builder
  namespace: story-builder
spec:
  type: ClusterIP
  selector:
    app: story-builder
  ports:
    - name: http
      port: 80
      targetPort: 8501
